# Tasks

## 1. Source de vérité et artefacts publiés

- [x] 1.1 Ajouter `VERSION` à `0.2.0` et vérifier que son contenu est la seule valeur de release déclarée.
- [x] 1.2 Aligner les manifestes des plugins `pllm` et `tfl` sur `0.2.0` et vérifier leur syntaxe JSON ainsi que leur égalité avec `VERSION`.

## 2. Documentation et validation

- [x] 2.1 Documenter la version `0.2.0` et sa source de vérité dans le README et les cadrages concernés, puis vérifier que ces références sont cohérentes.
- [x] 2.2 Exécuter la validation OpenSpec stricte du change et vérifier que toutes les tâches sont prouvées avant l'archivage.
